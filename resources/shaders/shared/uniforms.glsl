uniform vec3 cameraPosition;

const vec3 pointLightPos = vec3(1.0, 4.0, 2.0);
const vec3 pointLightColor = vec3(10.0, 10.0, 10.0);

const vec3 dirLightDir = vec3(-0.2, -1.0, -0.3);
const vec3 dirLightColor = vec3(1.0, 1.0, 1.0);